# 개발 일지 (DevLog)

## 서버 (Relay Server) 구조 및 기능

*   **역할**: 멀티플레이어 테트리스 게임을 위한 중계(Relay) 서버. 두 클라이언트 간의 매칭 및 패킷 릴레이 수행.
*   **통신 방식**: TCP 소켓 기반, `select()` 시스템 콜을 이용한 I/O 멀티플렉싱(단일 스레드 논블로킹).
*   **주요 사양**:
    *   최대 동시 접속 클라이언트 수: 64명 (`MAX_CLIENTS 64`)
    *   릴레이 버퍼 크기: 2048 바이트 (`RELAY_BUF 2048`)
*   **클라이언트 상태 관리 (ClientState)**:
    *   `CS_HELLO_WAIT`: 소켓 연결 직후, 첫 번째 `HELLO` 패킷(핸드셰이크) 수신 대기 상태.
    *   `CS_WAITING`: `HELLO` 패킷 정상 수신 후 파트너 매칭을 기다리는 대기 상태.
    *   `CS_MATCHED`: 매칭이 완료되어 서로 데이터를 릴레이하는 상태.
*   **매칭 (Matchmaking) 시스템**:
    *   클라이언트가 접속 시 `HELLO` 패킷과 함께 `roomName` (최대 32바이트) 전달.
    *   동일한 `roomName`으로 대기 중(`CS_WAITING`)인 다른 클라이언트를 찾아 매칭.
    *   `roomName`이 비어있을 경우 빠른 대전(quick match) 룸으로 간주.
    *   먼저 대기 중이던 클라이언트에게 'Host' 권한 부여 (난수 생성을 위한 Seed 권한 등).
*   **릴레이(Relay) 동작**:
    *   매칭이 완료된 두 클라이언트 사이에서, 서버는 데이터의 내용을 파싱하지 않고 단순 중계(`recv` 후 파트너에게 `sendAll`) 역할만 수행.
*   **예외 처리 및 서버 안정성**:
    *   `SIGPIPE` 시그널 무시(`SIG_IGN`): 연결이 끊긴 소켓에 전송을 시도할 때 서버 프로세스가 비정상 종료되는 것을 방지.
    *   Graceful Shutdown: `SIGINT`, `SIGTERM` 시그널 수신 시 안전하게 루프를 빠져나와 모든 소켓을 정리 후 종료.
    *   연결 해제 동기화: 릴레이 중 한쪽 클라이언트의 접속이 끊어지면, 매칭된 파트너의 소켓도 함께 종료하여 고아(orphan) 세션 방지.
    *   정원 초과 방어: 최대 수용 인원(64명) 초과 시 신규 접속자에게 `SRV_MSG_FULL` 메시지를 전송하고 즉시 소켓 차단.

